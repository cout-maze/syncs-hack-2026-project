# Frontend
VITE_API_URL=http://localhost:3000/api/v1

# Backend
PORT=3000
JWT_SECRET=rebuild-my-city-dev-secret-change-me
JWT_EXPIRES_IN=24h
CORS_ORIGIN=http://localhost:5173

# Optional. If unset, Advisor uses the canned fallback (fallback: true).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Demo login (seeded on first boot)
DEMO_EMAIL=demo@city.dev
DEMO_PASSWORD=rebuild-city
